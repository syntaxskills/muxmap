# Contributing

MuxMap requires Node.js 22+, tmux, and native build tools for `node-pty`.

```bash
npm install
npm test
npm run lint
npm run build
```

Keep changes focused, add the smallest regression test that covers non-trivial behavior, and update `CHANGELOG.md` under `Unreleased` for user-visible changes.
